// 用循环改写:不再占用调用栈,百万规模轻松完成
long long sumIter(int n) {
    long long s = 0;
    for (int i = 1; i <= n; ++i) s += i;
    return s;
}
// 识别"可改迭代"的标志:递归调用在函数末尾且结果直接返回
// (尾递归形态;编译器也可能自动优化,但不要依赖)
// 第3章会学到"显式栈"这一通用替代方案
