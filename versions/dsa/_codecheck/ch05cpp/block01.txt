// 负数取模示例:C/C++ 的 % 是截断除法,结果可正可负
int h = x % M;               // x = -5, M = 7 时 h = -5,不是 2!
cout << h;                   // 会打印 -5,负数下标会越界
if (h < 0)
    h += M;                  // 把负数修正到 [0, M-1]
return h;                    // 现在可以放心当数组下标用了
// 更省心的惯用写法:unsigned(x) % M 直接消掉负号
// 两种写法殊途同归:散列结果必须落在 [0, M-1]
