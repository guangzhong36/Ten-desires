// 开放定址:线性探测插入(空单元用 EMPTY 标记)
int pos = hashStr(s, M);
while (table[pos] != EMPTY && table[pos] != s)
    pos = (pos + 1) % M;      // 冲突就向后挪一格,越界绕回 0
if (table[pos] == EMPTY)
    table[pos] = s;           // 找到空位,插入
else
    ;                         // 键已存在,不做任何事
