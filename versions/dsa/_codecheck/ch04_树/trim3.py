# -*- coding: utf-8 -*-
P = r"C:\Users\zz666\signals_and_systems_tutor\dsa\cpp\src\ch04_树.html"
s = open(P, encoding="utf-8").read()
before = len(s.encode())

R = [
# 1 intuition P1
("树是\"有分支的链表\":链表结点只能排成一条线,树允许一个结点有多个后继,信息得以分层组织。家族谱、文件目录、语法树都是树。",
 "树是\"有分支的链表\":链表只能排成一条线,树允许一个结点有多个后继,信息分层组织。家族谱、文件目录、语法树都是树。"),
# 2 4.1.3
("一般树与二叉树可互相转换:C 的 first_child+next_sibling 就是\"长子变左子、兄弟变右子\"。",
 "一般树与二叉树可互相转换:\"长子变左子、兄弟变右子\"正是 C 的 first_child+next_sibling。"),
# 3 4.2.3
("第3章说过:任何递归都能改写成\"显式栈 + 循环\"。",
 "任何递归都能改写成\"显式栈 + 循环\"(第3章)。"),
# 4 后序思想
("三个经典问题都是\"后序思想\":先递归算完左右子树,再用结果组合出答案。基准情形都是 <code>n == nullptr</code> 返回 0,空树基准让\"单个叶结点\"自然成立;<span class=\"hl\">写树递归先问:空结点时该返回什么?</span>",
 "三个经典问题都是\"后序思想\":先递归算完左右子树,再组合出答案。基准情形都是 <code>n == nullptr</code> 返回 0,空树基准让\"单个叶结点\"自然成立;<span class=\"hl\">写树递归先问:空结点时该返回什么?</span>"),
# 5 4.4.2
("C 的经典做法是\"遍历到空位,修改父结点指针\";C++ 更稳的是<b>递归返回\"新根\"</b>——指针修改收拢在 <code>return</code> 里,父结点用返回值接住新根:",
 "C 的做法是\"遍历到空位,修改父结点指针\";C++ 更稳的是<b>递归返回\"新根\"</b>:指针修改收拢在 <code>return</code> 里,父结点用返回值接住:"),
# 6 误区5
("它会长成一条只有右孩子的链,查 1 要 O(n)。这不是理论病:自增主键、日志时间戳等\"按序到达\"的数据都会触发。解法:① 打乱插入顺序(治标);",
 "它长成一条只有右孩子的链,查 1 要 O(n)。这不是理论病:自增主键、日志时间戳等\"按序到达\"的数据都会触发。解法:① 打乱插入顺序;"),
# 7 4.6.1
("AVL 用平衡因子保证每次 O(log n),代价是代码复杂;伸展树不要平衡因子,只约定:每次访问后把该结点旋转\"伸展\"到根。",
 "AVL 用平衡因子保证每次 O(log n),代价是代码复杂;伸展树不要平衡因子,只约定:每次访问后把该结点\"伸展\"到根。"),
# 8 B树
("10<sup>7</sup> 条记录要下潜约 23 层;B 树是\"M 路分支\",树高降到 log<sub>M</sub> <i>n</i>,取 M = 1000 只需 3 层——<span class=\"hl\">\"浅而宽\"把 IO 次数从 20 多压到 3</span>。",
 "10<sup>7</sup> 条记录约 23 层;B 树是\"M 路分支\",树高降到 log<sub>M</sub> <i>n</i>,取 M = 1000 只需 3 层——<span class=\"hl\">\"浅而宽\"把 IO 从 20 多压到 3</span>。"),
# 9 4.7.2
("——比 AVL 更宽松的自平衡 BST(插入删除旋转更少),操作保证 <i>O</i>(log <i>n</i>),迭代器按序输出。细节第12章展开,先记住",
 "——比 AVL 更宽松的自平衡 BST(旋转更少),操作保证 <i>O</i>(log <i>n</i>),迭代器按序输出。第12章细讲,先记住"),
# 10 设计说明③
("③ 平衡检查写成\"返回(高度,是否平衡)\"的自底向上函数,一次递归算两件事,避免 O(n²);实测树高 7 合理,换 AVL 可压到约 5。",
 "③ 平衡检查是\"返回(高度,是否平衡)\"的自底向上函数,一次递归算两件事;实测树高 7,换 AVL 可压到约 5。"),
]

ok = 0
for old, new in R:
    if old in s:
        s = s.replace(old, new, 1)
        ok += 1
    else:
        print("MISS:", old[:45])
open(P, "w", encoding="utf-8").write(s)
print(f"applied {ok}/{len(R)}")
print(f"size {before} -> {len(s.encode())}  (need <=43008)")
