# -*- coding: utf-8 -*-
P = r"C:\Users\zz666\signals_and_systems_tutor\dsa\cpp\src\ch04_树.html"
s = open(P, encoding="utf-8").read()
before = len(s.encode())

R = [
# head
("能完整实现<span class=\"term\">BST</span> 的 contains/findMin/insert/remove(删除三情形)与 AVL 四种旋转,并用 <code>unique_ptr</code> 管理内存",
 "能完整实现<span class=\"term\">BST</span> 的 contains/findMin/insert/remove 与 AVL 四种旋转,并用 <code>unique_ptr</code> 管理内存"),
("树是\u201c有分支的链表\u201d:链表结点只能排成一条线,树允许一个结点有多个后继,信息得以分层组织。家族谱、文件目录、语法树都是树。",
 "树是\u201c有分支的链表\u201d:链表只能排成一条线,树允许一个结点有多个后继,信息分层组织。家族谱、文件目录、语法树都是树。"),
("C 里一般树靠双指针:firstChild + nextSibling 串起任意多个子结点,通用但笨拙。",
 "C 里一般树靠双指针 firstChild + nextSibling 串起任意多个子结点,通用但笨拙。"),
("第1章递归(遍历、高度、销毁全是递归)、第3章栈与队列(层序用队列,建表达式树用栈;中缀转后缀是 §4.3 的输入)",
 "第1章递归(遍历、高度、销毁)、第3章栈与队列(层序用队列,建表达式树用栈;中缀转后缀是 §4.3 的输入)"),
# §4.1
("树的递归定义:树是结点的集合——要么是空集,要么由一个<span class=\"term\">根</span>结点加上若干棵互不相交的<span class=\"term\">子树</span>构成,每棵子树本身也是树。这个定义直接决定了本章所有算法的递归写法。",
 "树的递归定义:树是结点的集合——要么是空集,要么由一个<span class=\"term\">根</span>结点加上若干棵互不相交的<span class=\"term\">子树</span>构成,每棵子树本身也是树。这决定了本章算法的递归写法。"),
("<span class=\"term\">深度</span>从根往下数(根 0),<span class=\"term\">高度</span>从叶往上数(叶 0),树高 = 根的高度;<span class=\"term\">路径</span>长度 = 边数。<span class=\"hl\">n 个结点的树恰好有 n−1 条边</span>。深度与高度数数方向相反,勿混淆。",
 "<span class=\"term\">深度</span>从根往下数(根 0),<span class=\"term\">高度</span>从叶往上数(叶 0),树高 = 根的高度;<span class=\"term\">路径</span>长度 = 边数。<span class=\"hl\">n 个结点的树有 n−1 条边</span>。方向相反,勿混淆。"),
("一般树与二叉树可互相转换:C 的 first_child+next_sibling 就是\u201c长子变左子、兄弟变右子\u201d。",
 "一般树与二叉树可互相转换:\u201c长子变左子、兄弟变右子\u201d正是 C 的 first_child+next_sibling。"),
("树的递归定义:树是结点的集合,要么是空集,\n要么由根结点加若干棵互不相交的子树构成。\n\n",
 "树的递归定义:树 = 根 + 互不相交的子树集合。\n"),
# §4.2
("第3章说过:任何递归都能改写成\u201c显式栈 + 循环\u201d。",
 "任何递归都能改写成\u201c显式栈 + 循环\u201d(第3章)。"),
("打印语句在两次递归之间的位置决定遍历类型——三序唯一的区别就是这行 <code>cout</code> 放哪。先用最小树手推一遍再对照输出。",
 "打印语句在两次递归之间的位置决定遍历类型——三序唯一的区别就是这行 <code>cout</code> 放哪。先用最小树手推一遍再对照。"),
("三个经典问题都是\u201c后序思想\u201d:先递归算完左右子树,再用结果组合出答案。基准情形都是 <code>n == nullptr</code> 返回 0,空树基准让\u201c单个叶结点\u201d自然成立;<span class=\"hl\">写树递归先问:空结点时该返回什么?</span>",
 "三个经典问题都是\u201c后序思想\u201d:先递归算完左右子树,再组合出答案。基准情形都是 <code>n == nullptr</code> 返回 0,空树基准让\u201c单个叶结点\u201d自然成立;<span class=\"hl\">写树递归先问:空结点时该返回什么?</span>"),
# §4.3
("运算符结点的左右子树就是两个操作数——后缀式 <code>6 5 2 3 + * +</code> 对应:",
 "左右子树就是两个操作数——后缀式 <code>6 5 2 3 + * +</code> 对应:"),
("构建算法与第3章后缀求值几乎同构:",
 "构建与第3章后缀求值几乎同构:"),
# §4.4
("查找 = 比较后决定走左走右,一路下行丢弃一半子树——二分查找的树形版,且插入删除不用搬移元素:",
 "查找 = 比较后走左或右,一路下行丢掉一半子树——二分查找的树形版,插入删除不用搬移元素:"),
("C 的经典做法是\u201c遍历到空位,修改父结点指针\u201d;C++ 更稳的是<b>递归返回\u201c新根\u201d</b>——指针修改收拢在 <code>return</code> 里,父结点用返回值接住新根:",
 "C 的做法是\u201c遍历到空位,修改父结点指针\u201d;C++ 更稳的是<b>递归返回\u201c新根\u201d</b>:指针修改收拢在 <code>return</code> 里,父结点用返回值接住:"),
("——它大于左子树全部、小于右子树其余,且无左子树,删除退化为情形①②:",
 "——它大于左子树全部、小于右子树其余,且无左子树:"),
("① <b>漏接右子树:</b>只写 <code>n-&gt;val = m-&gt;val</code> 就 <code>return n</code>,<code>m</code> 还挂在右子树里——两个相同值且泄漏,必须再递归删除 <code>m</code>。② <b>删错目标:</b>写成 <code>n-&gt;right = remove(n-&gt;right, x)</code> 删除扑空。<span class=\"hl\">先复制,再用 m 删右子树</span>。",
 "① <b>漏接右子树:</b>只写 <code>n-&gt;val = m-&gt;val</code> 就 <code>return n</code>,<code>m</code> 还挂在右子树里——重复且泄漏,必须再递归删除 <code>m</code>。② <b>删错目标:</b>写成 <code>n-&gt;right = remove(n-&gt;right, x)</code> 必扑空。<span class=\"hl\">先复制,再用 m 删右子树</span>。"),
("但最坏是灾难:数据<b>恰好有序</b>插入时树退化成链,查找变线性扫描。",
 ";数据<b>恰好有序</b>插入时树退化成链,查找变线性扫描。"),
("它会长成一条只有右孩子的链,查 1 要 O(n)。这不是理论病:自增主键、日志时间戳等\u201c按序到达\u201d的数据都会触发。解法:① 打乱插入顺序(治标);",
 "它长成一条只有右孩子的链,查 1 要 O(n)。这不是理论病:自增主键、日志时间戳等\u201c按序到达\u201d的数据都会触发。解法:① 打乱插入顺序;"),
# §4.5
("插入可能把某祖先推到 ±2,就用<b>旋转</b>扳回。旋转分四种,看失衡结点的哪个孩子更高:",
 "插入可能把某祖先推到 ±2,用<b>旋转</b>扳回,分四种,看失衡结点的哪个孩子更高:"),
("所有旋转<b>不破坏 BST 的大小序</b>——中序遍历结果不变。",
 "<b>旋转不破坏 BST 的大小序</b>——中序遍历结果不变。"),
("<div class=\"box box-example\"><div class=\"box-title\">例题 4-2 手走一次 LL 旋转</div>\n<p>空树依次插入 3、2、1:结点 3 的 bf = 2,1 在 3 的左孩子 2 的左子树 → LL,右旋:2 提为根,3 变 2 的右孩子。再插 4、5:结点 3 的 bf = −2 → RR。最终:根 2,左 1,右 4(左 3 右 5)。</p>\n</div>",
 "<p><b>例题 4-2(手走旋转):</b>空树依次插入 3、2、1:结点 3 的 bf=2 且落在左孩子 2 的左子树 → LL,右旋:2 提为根,3 变其右孩子;再插 4、5,结点 3 的 bf=−2 → RR。最终:根 2,左 1,右 4(左 3 右 5)。</p>"),
("LL:bf(k2)=+2 且 k1 左重;RR:bf(k2)=-2 且 k1 右重;\nLR:bf(k2)=+2 且 k1 右重;RL:bf(k2)=-2 且 k1 左重。",
 "LL:bf=+2 且左重;RR:bf=-2 且右重;LR:bf=+2 且右重;RL:bf=-2 且左重。"),
# §4.6
("AVL 用平衡因子保证每次 O(log n),代价是代码复杂;伸展树不要平衡因子,只约定:每次访问后把该结点旋转\u201c伸展\u201d到根。",
 "AVL 用平衡因子保证每次 O(log n),代价是代码复杂;伸展树不要平衡因子,只约定:每次访问后把该结点\u201c伸展\u201d到根。"),
("结论(证明在第11章摊还分析):单次最坏 O(n),连续 m 次操作总代价 O(m log n),<span class=\"hl\">摊还 O(log n)</span>;反复访问的结点总停在根附近(自愈),但单次最坏延迟无法保证。",
 "结论(证明在第11章摊还分析):单次最坏 O(n),连续 m 次操作总代价 O(m log n),<span class=\"hl\">摊还 O(log n)</span>;反复访问的结点总停在根附近,但单次最坏延迟无法保证。"),
("10<sup>7</sup> 条记录要下潜约 23 层;B 树是\u201cM 路分支\u201d,树高降到 log<sub>M</sub> <i>n</i>,取 M = 1000 只需 3 层——<span class=\"hl\">\u201c浅而宽\u201d把 IO 次数从 20 多压到 3</span>。",
 "10<sup>7</sup> 条记录约 23 层;B 树是\u201cM 路分支\u201d,树高降到 log<sub>M</sub> <i>n</i>,取 M = 1000 只需 3 层——<span class=\"hl\">\u201c浅而宽\u201d把 IO 从 20 多压到 3</span>。"),
# §4.7
("——比 AVL 更宽松的自平衡 BST(插入删除旋转更少),操作保证 <i>O</i>(log <i>n</i>),迭代器按序输出。细节第12章展开,先记住",
 "——比 AVL 更宽松的自平衡 BST(旋转更少),操作保证 <i>O</i>(log <i>n</i>),迭代器按序输出。第12章细讲,先记住"),
("手写 <code>~BST() { delete root_; }</code> 时析构是递归的:退化链(树高 n)递归 n 层直接撞栈上限;两棵树共享结点还会 double free。用 <code>unique_ptr</code> 后:① 深树销毁仍可能爆栈,改用显式栈迭代销毁(思路同 §4.2.3);② <code>unique_ptr</code> 禁止拷贝,共享树换 <code>shared_ptr</code>。",
 "手写 <code>~BST() { delete root_; }</code> 时析构是递归的:退化链递归 n 层直接撞栈上限;共享结点会 double free。用 <code>unique_ptr</code> 后:① 深树销毁仍可能爆栈,可改显式栈迭代销毁(§4.2.3);② 禁止拷贝,共享树换 <code>shared_ptr</code>。"),
# 自测题
("n−1 条:除根外每个结点都有且只有一个父结点。深度从根往下数(根 0),高度从叶往上数(叶 0);树高 = 根的高度。",
 "n−1 条:除根外每个结点都有一个父结点。深度从根往下(根 0),高度从叶往上(叶 0);树高 = 根的高度。"),
("构建:6、5、2 入栈;- 弹 2、5 得 (5−2);3 入栈;* 得 (5−2)*3;+ 得 6+((5−2)*3)。先弹的是右操作数,所以是 5−2 而非 2−5。",
 "构建:6、5、2 入栈;- 弹 2、5 得 (5−2);3 入栈;* 得 (5−2)*3;+ 得 6+((5−2)*3)。先弹的是右操作数,故是 5−2 而非 2−5。"),
("右子树最小结点 m 大于左子树全部、小于右子树其余,顶替后性质不变;且 m 没有左子树,删除退化为单子/叶情形。取左子树最大结点同样可行,教材惯例取右子树最小。",
 "右子树最小结点 m 大于左子树全部、小于右子树其余,顶替后性质不变;且 m 没有左子树,删除退化为单子/叶情形。取左子树最大结点也可行,教材惯例取右子树最小。"),
# 设计说明
("① 五个任务对应五条主线:分词在 §4.1,插入/删除在 §4.4 三情形,中序遍历在 §4.2,平衡检查与树高在 §4.5,串成可运行的流水线;",
 "① 任务覆盖本章主线:分词(§4.1)、插入/删除三情形(§4.4)、中序遍历(§4.2)、平衡检查(§4.5),串成可运行的流水线;"),
("② 结点用 <code>unique_ptr</code> 持有子树,类无需析构函数,双子删除 <code>return move(n-&gt;left)</code> 立即释放孤儿结点(§4.7);",
 "② 结点用 <code>unique_ptr</code> 持有子树,类无需析构函数,双子删除 <code>return move(n-&gt;left)</code> 即释放孤儿结点(§4.7);"),
("③ 平衡检查写成\u201c返回(高度,是否平衡)\u201d的自底向上函数,一次递归算两件事,避免 O(n²);实测树高 7 合理,换 AVL 可压到约 5。",
 "③ 平衡检查是\u201c返回(高度,是否平衡)\u201d的自底向上函数,一次递归算两件事;实测树高 7,换 AVL 可压到约 5。"),
]

ok = 0
for old, new in R:
    if old in s:
        s = s.replace(old, new, 1)
        ok += 1
    else:
        print("MISS:", old[:45])
open(P, "w", encoding="utf-8").write(s)
print(f"applied {ok}/{len(R)}")
print(f"size {before} -> {len(s.encode())}  (need <=43008)")
