#include <algorithm>
vector<int> a = {1, 3, 5, 7, 9};
bool hit = binary_search(a.begin(), a.end(), 5);      // true
// lower_bound:第一个 >= x 的位置(不存在则返回 end())
auto it = lower_bound(a.begin(), a.end(), 6);         // 指向 7
int pos = int(it - a.begin());                        // 3
// upper_bound:第一个 > x 的位置,两者之差即"等于 x 的元素个数"
int cnt = int(upper_bound(a.begin(), a.end(), 5) - it);
