# -*- coding: utf-8 -*-
"""ch12 C++ 版一次性压缩脚本:每个替换必须唯一匹配,否则报错退出。"""
import sys

P = r"C:\Users\zz666\signals_and_systems_tutor\dsa\cpp\src\ch12_高级数据结构.html"
html = open(P, encoding="utf-8").read()
orig = len(html)

REPL = [
# ---- 正文压缩 ----
("——<span class=\"hl\">最长 ≤ 最短的 2 倍,树高 <i>O</i>(log <i>n</i>),三操作最坏 <i>O</i>(log <i>n</i>)</span>。这就是\"弱平衡\"的精髓:不追求 AVL 的高度差 ≤ 1,只保证\"不会太歪\"。</p>",
 "——<span class=\"hl\">最长 ≤ 最短的 2 倍,树高 <i>O</i>(log <i>n</i>),三操作最坏 <i>O</i>(log <i>n</i>)</span>。这就是\"弱平衡\":不追求 AVL 的高度差 ≤ 1。</p>"),
("在 C 里做\"有序字典\"很痛苦:手写 AVL/红黑树要几百行,任何一个修正分支写错就悄悄失衡。C++ 的答案是一行 <code>std::map</code>——内部就是红黑树,按键有序、支持范围查询、迭代器语义严谨。</p>",
 "在 C 里做\"有序字典\"很痛苦:手写 AVL/红黑树要几百行,一个修正分支写错就悄悄失衡;C++ 的答案是一行 <code>std::map</code>——内部就是红黑树。</p>"),
("却拿到期望 <i>O</i>(log <i>n</i>) 查找——用\"概率上几乎不可能坏\"换\"写起来几乎不可能错\",Redis 的 zset 底层就是跳表。</p>",
 "却拿到期望 <i>O</i>(log <i>n</i>)——用\"概率上几乎不可能坏\"换\"写起来几乎不可能错\"。</p>"),
("读 §12.1–§12.4,背下红黑树修正口诀,手写 Treap、跳表与 k-d 树",
 "读 §12.1–§12.4,背下修正口诀,手写 Treap、跳表与 k-d 树"),
("旋转后被提上来的结点染黑、爷爷染红,修正结束。</p>",
 "旋转后被提上来的结点染黑、爷爷染红。</p>"),
("删除要把黑高\"挖掉\"一层,处理\"双黑\"问题,分支多得多——<span class=\"hl\">红黑树\"实现极复杂、接口极简单\",删除从不手写,正因如此才适合内建在标准库</span>。",
 "删除要把黑高\"挖掉\"一层,处理\"双黑\"问题——<span class=\"hl\">红黑树\"实现极复杂、接口极简单\",删除从不手写,因此适合内建在标准库</span>。"),
("——把 2-3-4 树一个结点内的 2~3 个键\"压扁\"成红黑交替的二叉链,红结点与黑父逻辑上属于同一个 2-3-4 结点。",
 "——把 2-3-4 树一个结点内的 2~3 个键\"压扁\"成红黑交替的二叉链。"),
("——黑结点孩子颜色任意。另一高频错误是口诀记反:叔叔红时去旋转会得到一棵永远修不好的树。",
 "——黑结点孩子颜色任意。高频错误还有口诀记反:叔叔红时去旋转,树永远修不好。"),
("插入:先按 BST 规则插到叶位置(带随机优先级),再沿路径向上旋转,直到父的优先级 ≤ 子——与二叉堆\"上滤\"异曲同工,只是靠旋转完成。删除同理:把目标结点旋转着推下去,变成叶子再摘除。",
 "插入:先按 BST 规则插到叶位置,再沿路径向上旋转,直到父的优先级 ≤ 子——与二叉堆\"上滤\"异曲同工。删除同理:把目标旋转着推下去,变叶子再摘除。"),
("注意:两结点优先级相等时要约定比较方向,否则可能破坏 BST 序。",
 "优先级相等时须约定比较方向,否则可能破坏 BST 序。"),
("本例用小顶堆(父 ≤ 子),口诀\"左小右旋、右小左旋\"。若定义成大顶堆,所有比较都要取反;混用两套口径会得到键序与堆序打架的树。实现后用有序序列插入再中序遍历:输出必须升序,且每个结点优先级 ≤ 子结点。",
 "本例用小顶堆(父 ≤ 子),口诀\"左小右旋、右小左旋\";若定义成大顶堆,所有比较都要取反。实现后用有序序列插入再中序遍历:输出必须升序,且每个结点优先级 ≤ 子结点。"),
("查找从最高层出发,每层向右走到\"下一个比目标大\"的结点前再下降一层,共 <i>O</i>(log <i>n</i>) 层。",
 "查找从最高层出发,每层右移到\"下一个更大\"结点前再下降,共 <i>O</i>(log <i>n</i>) 层。"),
("<td>高度</td><td>期望 <i>O</i>(log <i>n</i>),最坏 <i>O</i>(<i>n</i>)(概率几乎为 0)</td>",
 "<td>高度</td><td>期望 <i>O</i>(log <i>n</i>),最坏 <i>O</i>(<i>n</i>)</td>"),
("要快速实现选跳表——跳表还天然便于无锁并发修改,红黑树做不到。</p>",
 "要快速实现选跳表——跳表还天然便于无锁并发修改。</p>"),
("③ 用 <code>time()</code> 做种子,同一秒插入的结点层数高度相关。调试:打印每一层的链表内容,验证\"第 0 层完整、高层稀疏\"。",
 "③ 用 <code>time()</code> 做种子,同一秒插入的结点层数高度相关。调试:逐层打印链表,验证\"第 0 层完整、高层稀疏\"。"),
("左子树是该维度小于切分点的点集,右子树反之;下一层换另一个维度。",
 "左子树为小于切分点的点集,右子树反之;下一层换另一个维度。"),
("注意:数据退化成\"都挤在一条直线上\"时剪枝几乎失效,查询最坏 <i>O</i>(<i>n</i>);生产级替代是 R 树或网格索引。",
 "数据挤在一条直线上时剪枝失效,查询最坏 <i>O</i>(<i>n</i>);生产级替代是 R 树或网格索引。"),
("调试:拿 5 点小数据集,把\"近侧/远侧/是否剪枝\"逐结点打印核对。",
 "调试:拿 5 点小数据集逐结点打印\"近侧/远侧/是否剪枝\"核对。"),
("要 <i>O</i>(<i>n</i>)(拷贝数组)。左式堆",
 "要 <i>O</i>(<i>n</i>)。左式堆"),
("配对堆的代价是摊还而非最坏:单次 deleteMin 可能偶发 <i>O</i>(<i>n</i>),但长时间平均 <i>O</i>(log <i>n</i>)。",
 "代价是摊还而非最坏:单次 deleteMin 可能偶发 <i>O</i>(<i>n</i>),长时间平均 <i>O</i>(log <i>n</i>)。"),
# 12.3.3 表格合并两行 + 删除选型直觉句
("<tr><td>平衡机制</td><td>随机层数(概率平衡)</td><td>旋转 + 不变式(确定平衡)</td></tr>\n<tr><td>实现难度</td><td>≈ 链表级别,几十行</td><td>高,数百行</td></tr>",
 "<tr><td>平衡机制 / 实现难度</td><td>随机层数,≈ 链表级,几十行</td><td>旋转 + 不变式,高,数百行</td></tr>"),
("<p>选型直觉:<span class=\"hl\">确定性与实现难度不可兼得</span>。要最坏保证(实时系统)选红黑树;要快速实现选跳表——跳表还天然便于无锁并发修改。</p>",
 ""),
# 删除 12.6.1 三场景推荐 box(与决策表重复)
("<div class=\"box box-keypoint\"><div class=\"box-title\">重点:三场景推荐</div>\n<p>① <b>纯查找/去重</b> → unordered_map/unordered_set;② <b>有序遍历/前驱后继</b> → map/set;③ <b>范围查询</b> → map 的 <code>lower_bound</code>/<code>upper_bound</code> 定位。口诀:看到\"排序\"\"范围\"\"前驱\"就直接 map。</p>\n</div>\n\n",
 ""),
("持续插入 + 需要有序:map(手写红黑树永远不是选项,除非你在写标准库)",
 "持续插入 + 需要有序:map(手写红黑树永远不是选项)"),
("只查不遍历:unordered_map;哈希冲突导致性能劣化时换更均匀的哈希",
 "只查不遍历:unordered_map;冲突多时换更均匀的哈希"),
("Python 同学没有 map:小数据 list+bisect,批处理 dict+sorted,大数据上第三方 sortedcontainers(见 Python 版 §12.6)",
 "Python 同学没有 map:list+bisect、dict+sorted、sortedcontainers(见 Python 版 §12.6)"),
("② <b>系统设计</b>——缓存用散列、排行榜用跳表(Redis zset)、范围索引用 B+ 树;③ <b>数据库索引</b>——B+ 树是红黑树的\"磁盘亲戚\",只需补充\"结点分裂/合并\"。",
 "② <b>系统设计</b>——缓存用散列、排行榜用跳表、范围索引用 B+ 树;③ <b>数据库索引</b>——B+ 树是红黑树的\"磁盘亲戚\"。"),
("——与快排随机枢轴的分析同构。最坏是优先级恰好按键单调,高度 <i>O</i>(<i>n</i>),概率 1/<i>n</i>!,现实几乎不可能。",
 "——与快排随机枢轴同构。最坏是优先级恰好按键单调,高度 <i>O</i>(<i>n</i>),概率 1/<i>n</i>!。"),
("层数必须几何分布(每层 1/2 概率晋升):每层期望结点数 <i>n</i>/2<sup>k</sup>",
 "几何分布(每层 1/2 概率晋升)使每层期望结点数 <i>n</i>/2<sup>k</sup>"),
("漏写剪枝退化成全树搜索 <i>O</i>(<i>n</i>);方向写反则可能跳过真正更近的点。",
 "漏写则退化成全树搜索 <i>O</i>(<i>n</i>);方向写反则可能漏掉更近的点。"),
("\"遍历中插入\"或\"缓存迭代器\"的场景必须优先 map——§3.3 迭代器失效思想在本章的再次登场。",
 "\"遍历中插入\"或\"缓存迭代器\"必须优先 map——§3.3 迭代器失效思想再现。"),
("任务 3 用同一颗种子生成随机操作序列,插入前先 find 去重(与 map 键唯一语义对齐);任务 4 洗牌生成 10<sup>4</sup> 个不重复键,结果累加输出防死代码消除",
 "任务 3 用同一种子生成随机操作序列,插入前先 find 去重;任务 4 洗牌生成 10<sup>4</sup> 个不重复键,结果累加输出防死代码消除"),
("③ 任务3 用 20000 次随机操作同时驱动跳表与 map,任何一次查找结论不一致立即退出——\"验证优先于推测\";④ 计时用同一份键序列,两者同为 <i>O</i>(log <i>n</i>),map 常数更小,跳表胜在实现简单;",
 "③ 任务3 用同一随机操作序列同时驱动两者,任何一次查找不一致立即退出;④ 同一份键序列计时,两者同为 <i>O</i>(log <i>n</i>),map 常数更小;"),
("全书十二章到此结束。回望整条学习曲线:第1–2章打下递归与复杂度的地基,第3–9章建起线性结构、树、散列、堆、排序、图的完整工具箱,第10–11章学会设计范式与摊还分析两把\"思维扳手\",本章则把最后一块拼图——平衡与概率——放进工具箱。感谢你读到这一行:数据结构是算法工程师的母语,愿你在算法竞赛、系统设计或工程实践中,带着这十二章的判断力走得更远。",
 "全书十二章到此结束:第1–2章打下递归与复杂度地基,第3–9章建起线性结构、树、散列、堆、排序、图的工具箱,第10–11章学会设计范式与摊还分析,本章补上最后一块拼图——平衡与概率。数据结构是算法工程师的母语,愿你在竞赛、系统设计或工程实践中带着这十二章的判断力走得更远。"),
("能独立实现 Treap 与跳表的插入/删除/查找,说出层数为何几何分布、期望为何 <i>O</i>(log <i>n</i>)",
 "能独立实现 Treap 与跳表的插入/删除/查找,说出期望为何 <i>O</i>(log <i>n</i>)"),
("能说出\"红黑树是 2-3-4 树的二叉表示\"这一句话关系",
 "能说出\"红黑树是 2-3-4 树的二叉表示\""),
("能说出配对堆合并摊还 <i>O</i>(1)、deleteMin 摊还 <i>O</i>(log <i>n</i>),一句话对比二叉堆/左式堆",
 "能说出配对堆合并摊还 <i>O</i>(1)、deleteMin 摊还 <i>O</i>(log <i>n</i>),一句话对比三堆"),
("第1章 绪论:递归、log 与数学基础 → 第2章 算法分析:<i>O</i>/<i>Ω</i>/<i>Θ</i> 记号",
 "第1章 绪论:递归与数学 → 第2章 算法分析:<i>O</i>/<i>Ω</i>/<i>Θ</i>"),
("第3章 表、栈、队列:动态数组与链表 → 第4章 树:BST、AVL、遍历",
 "第3章 表栈队列:数组与链表 → 第4章 树:BST、AVL、遍历"),
("第5章 散列:冲突处理 → 第6章 优先队列(堆):二叉堆、上滤下滤",
 "第5章 散列:冲突处理 → 第6章 堆:二叉堆、上滤下滤"),
("第7章 排序:八大排序与比较下界 → 第8章 不相交集:等价类与 union/find",
 "第7章 排序:八大排序 → 第8章 不相交集:union/find"),
("第9章 图:最短路、MST、拓扑排序 → 第10章 算法设计:贪心、分治、DP、回溯",
 "第9章 图:最短路与 MST → 第10章 算法设计:贪心、分治、DP"),
("第11章 摊还分析:聚集、会计、势能 → 第12章 高级结构:平衡与期望",
 "第11章 摊还分析:三方法 → 第12章 高级结构:平衡与期望"),
("第4章树(BST、旋转、AVL)、第5章散列、第6章堆、第2章 <i>O</i> 记号、第11章摊还分析。",
 "第4章树(BST、旋转、AVL)、第5章散列、第6章堆、第11章摊还分析。"),
("键是自定义结构时,map 需要\"严格弱序\"(等价的键互相比较为假),unordered_map 需要哈希函数与判等:",
 "键是自定义结构时,map 需要\"严格弱序\"(等价的键互比为假),unordered_map 需要哈希与判等:"),
("把 §12.3 的跳表包一层,接口与 <code>std::map</code> 完全对齐——这正是第3章 ADT 的威力:",
 "把 §12.3 的跳表包一层,接口与 <code>std::map</code> 对齐——第3章 ADT 的威力:"),
("<h3>12.6.4 工程选型的完整建议</h3>",
 "<h3>12.6.4 工程选型建议</h3>"),
("数据量小且只做一次性有序输出:vector + <code>sort</code>",
 "一次性有序输出:vector + <code>sort</code>"),
("按示例 12-7/12-8 实现 SkipList,用升序/降序/乱序三组输入冒烟测试,遍历必须升序",
 "按示例 12-7/12-8 实现 SkipList,升/降/乱序三组输入冒烟测试,遍历必须升序"),
("① 跳表用 <code>update</code> 数组统一\"下探路径\",插入/删除共享骨架;② 插入前先 <code>find</code> 去重,与 <code>std::map</code> 键唯一语义对齐;",
 "① <code>update</code> 数组统一\"下探路径\",插入/删除共享骨架;② 插入前先 <code>find</code> 去重,与 map 键唯一语义对齐;"),
# ---- 代码注释压缩 ----
("};\n\n\nNode* rotateRight(Node* t) {",
 "};\n\nNode* rotateRight(Node* t) {"),
("    int key, pri;               // 键与随机优先级\n",
 "    int key, pri;\n"),
("    else if (t-&gt;left-&gt;pri &lt; t-&gt;right-&gt;pri) {   // 优先级小的旋上来\n",
 "    else if (t-&gt;left-&gt;pri &lt; t-&gt;right-&gt;pri) {\n"),
("            update[i] = cur;                   // 每层最后一个 &lt; key\n",
 "            update[i] = cur;\n"),
("        for (int i = 0; i &lt;= lvl; ++i) {       // 逐层链入\n",
 "        for (int i = 0; i &lt;= lvl; ++i) {\n"),
("        if (find(key)) return;                 // 字典语义:键唯一\n",
 "        if (find(key)) return;\n"),
("    Node* head_;               // 头结点:拥有全部层的起点\n",
 "    Node* head_;               // 头结点:全部层的起点\n"),
("    mt19937 rng_{12345};       // 固定种子,便于复现\n",
 "    mt19937 rng_{12345};       // 固定种子\n"),
("            if (update[i]-&gt;next[i] != x) break;  // 该层没有 x\n",
 "            if (update[i]-&gt;next[i] != x) break;\n"),
("            --level_;                          // 回收空高层\n",
 "            --level_;\n"),
("    int dim = depth % 2;                       // 维度交替\n",
 "    int dim = depth % 2;\n"),
("// nth_element:O(n) 选出第 mid 小的元素并原地分区\n",
 "// nth_element:O(n) 选出中位数并原地分区\n"),
("    KdNode* near = (qc &lt; cut) ? node-&gt;left : node-&gt;right;  // 先探近侧\n",
 "    KdNode* near = (qc &lt; cut) ? node-&gt;left : node-&gt;right;\n"),
("    double dCut = (qc - cut) * (qc - cut);          // q 到分割线距离\n",
 "    double dCut = (qc - cut) * (qc - cut);\n"),
("    if (dCut &lt; bestD)                               // 剪枝:够近才跨过去\n",
 "    if (dCut &lt; bestD)\n"),
("    b-&gt;next = a-&gt;child;                      // b 挂为 a 的第一个孩子\n",
 "    b-&gt;next = a-&gt;child;\n"),
("PHNode* mergePairs(PHNode* first) {          // 两趟合并:摊还 O(log n)\n",
 "PHNode* mergePairs(PHNode* first) {          // 两趟合并\n"),
("    delete root;                             // 根即最小,直接摘掉\n",
 "    delete root;\n"),
("    return mergePairs(children);             // 剩余孩子两两合并\n",
 "    return mergePairs(children);\n"),
("// insert:merge(现有堆, 单结点新堆),即摊还 O(1)",
 "// insert = merge(现堆, 单结点堆),摊还 O(1)"),
("// ---- 手写版:§12.3 的 SkipList 包成有序字典 ----",
 "// ---- 手写版:SkipList 包成有序字典 ----"),
("// ---- 标准库版:同一套接口,红黑树实现 ----",
 "// ---- 标准库版:同一套接口,红黑树 ----"),
("    // ---- 任务 3:与 std::map 对照,输出必须一致 ----",
 "    // 任务 3:与 std::map 对照,输出必须一致"),
("        int op = i % 4;                 // 0/1 插入,2 删除,3 查找",
 "        int op = i % 4;                 // 0/1 插,2 删,3 查"),
("    cout &lt;&lt; \"跳表前 10 键: \";\n    for (int i = 0; i &lt; 10; ++i) cout &lt;&lt; dk[i] &lt;&lt; ' ';\n    cout &lt;&lt; \"\\nmap  前 10 键: \";\n    for (int i = 0; i &lt; 10; ++i) cout &lt;&lt; mk[i] &lt;&lt; ' ';\n    cout &lt;&lt; \"\\n按序遍历顺序一致: \" &lt;&lt; (dk == mk ? \"是\" : \"否\") &lt;&lt; endl;",
 "    cout &lt;&lt; \"跳表前 10 键: \";\n    for (int i = 0; i &lt; 10; ++i) cout &lt;&lt; dk[i] &lt;&lt; ' ';\n    cout &lt;&lt; \"\\n按序遍历顺序一致: \" &lt;&lt; (dk == mk ? \"是\" : \"否\") &lt;&lt; endl;"),
("    // ---- 任务 4:10^4 级随机键计时对比 ----",
 "    // 任务 4:10^4 随机键计时对比"),
("    shuffle(ks.begin(), ks.end(), rng);        // 洗牌为随机序列",
 "    shuffle(ks.begin(), ks.end(), rng);"),
("    // ---- 任务 5:删除全部元素,验证跳表为空 ----",
 "    // 任务 5:删除全部元素"),
("跳表前 10 键: 0 1 2 3 4 6 8 9 10 12\nmap  前 10 键: 0 1 2 3 4 6 8 9 10 12\n按序遍历顺序一致: 是",
 "跳表前 10 键: 0 1 2 3 4 6 8 9 10 12\n按序遍历顺序一致: 是"),
]

for i, (old, new) in enumerate(REPL):
    n = html.count(old)
    if n != 1:
        print(f"[{i}] 匹配 {n} 次,跳过: {old[:60]!r}")
        continue
    html = html.replace(old, new)

open(P, "w", encoding="utf-8").write(html)
print(f"原大小 {orig} → 新大小 {len(html)} (省 {orig - len(html)}B)")
